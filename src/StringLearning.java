public class StringLearning {
    public static void main(String[] args) {

        String name = "lanos institute of technologies";
        System.out.println(name);
        String newName;
        // working methods of string class:

//        newName = name.toLowerCase(); // converts the string into small letters
//        System.out.println(newName);

//        newName = name.toUpperCase(); // converts the string into capital letters
//        System.out.println(newName);

//        newName = name.concat(" das pathan"); // merges the old string with new string
//        System.out.println(newName);

//        System.out.println(name.isEmpty()); // checks if the string is empty or not.

//        System.out.println(name.startsWith("lanos")); // checks if the string starts with particular string phrase or not.

//        System.out.println(name.indexOf("technologies")); // returns the position/index of the char/string
//        System.out.println(name.lastIndexOf('s')); // returns the position/index of the char/string searching from the last.
//        System.out.println(name.charAt(17)); // returns the character at the particular position.
//        System.out.println(name.endsWith("s"));// returns the boolean result on the basis of end letter mathing with given string.
//
    }
}
